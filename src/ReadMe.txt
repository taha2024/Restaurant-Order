Instructions to start program:

1. Open assignment3 directory in terminal and use the command 'npm install express' and 'npm install pug'
2. Run 'node server.js' to start server
3. Open 'http://localhost:3000/' in web browser to open web page